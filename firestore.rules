rules_version = '2';

// All legitimate reads and writes go through the Sardha Express API
// using the Firebase Admin SDK, which bypasses these rules entirely.
// Direct client access is therefore safe to block completely.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
